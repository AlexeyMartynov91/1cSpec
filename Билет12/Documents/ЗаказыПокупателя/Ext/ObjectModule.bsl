
Процедура ОбработкаПроведения(Отказ, Режим)
	// регистр Заказы Приход
	Движения.Заказы.Записывать = Истина;
	Движения.Заказы.Записать();
	Для Каждого стр Из СписокНоменклатуры Цикл
		Движение = Движения.Заказы.ДобавитьПриход();
		ЗаполнитьЗначенияСвойств(Движение, стр);
		Движение.Период = Дата;
		Движение.Заказ = Ссылка;
	КонецЦикла;

КонецПроцедуры
