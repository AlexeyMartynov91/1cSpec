
&НаКлиенте
Процедура КомментарийОткрытие(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка	= Ложь;
	
	СтрокаПоиска	= Элемент.ТекстРедактирования;  
	Позиция	= Найти(СтрокаПоиска, "e1cib/data/Документ");
	Если Позиция > 0 Тогда
		НавигацСсылка	= Сред(СтрокаПоиска, Позиция);
		ПерейтиПоНавигационнойСсылке(НавигацСсылка);
	КонецЕсли;
	
КонецПроцедуры
